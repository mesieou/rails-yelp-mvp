<%= link_to "Add new restaurant", new_restaurant_path, class: 'm-3'  %>

<div class="container mt-3">
  <h1>Restaurants:</h1>
  <ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name,restaurant_path(restaurant)%>
    </li>
  <% end %>
  </ul>
</div>
